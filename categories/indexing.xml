<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet type="text/xsl" href="../assets/xml/rss.xsl" media="all"?><rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>SM (Posts about indexing)</title><link></link><description></description><atom:link href="categories/indexing.xml" rel="self" type="application/rss+xml"></atom:link><language>en</language><copyright>Contents © 2023 &lt;a href="mailto:me@sebastianmontero.com"&gt;Sebastian Montero&lt;/a&gt; </copyright><lastBuildDate>Tue, 31 Oct 2023 00:15:11 GMT</lastBuildDate><generator>Nikola (getnikola.com)</generator><docs>http://blogs.law.harvard.edu/tech/rss</docs><item><title>#8 Index compression</title><link>posts/notes-on-information-retrieval-5/</link><dc:creator>Sebastian Montero</dc:creator><description>&lt;blockquote&gt;
&lt;p&gt;This is a blog series on Information Retrieval covering the first chapters of the textbook &lt;a href="https://nlp.stanford.edu/IR-book/information-retrieval-book.html"&gt;Introduction to Information Retrieval&lt;/a&gt;. This post covers &lt;strong&gt;Chapter 5: Index compression&lt;/strong&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;This post will cover various ways for index compression to that are crucial for an efficient IR system. The main benefit of index compression is that we need less risk space - we can cut down storage costs by up to 75%.&lt;/p&gt;
&lt;p&gt;Other benefits of index compression include increased used of caching and faster data transfers. In production search systems, some parts will be used more than others - here's where we take advantage of increased caching - we ensure that the most frequent searches are cached, and because we have a compressed index we can also ensure that we fit a lot of information into main memory and we have no need to search the main disk. As we compress data, we also have faster data transfers from disk to memory.&lt;/p&gt;
&lt;p&gt;To ensure we take advantage of these two benefits we need to ensure that decompression speeds are high. The following methods ensure this.&lt;/p&gt;
&lt;h3&gt;Statistical properties of terms in IR&lt;/h3&gt;
&lt;p&gt;In general preprocessing will affect the final size of the dictionary. Processing steps like stemming and case folding reduce terms significantly.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;rule of 30&lt;/code&gt; treats the most frequent words and states that the 30 most common words account for 30% of written text. Because of this, some literature recommends eliminating the most common words to reduce the size significantly.&lt;/p&gt;
&lt;p&gt;This section covers two &lt;code&gt;lossless compression&lt;/code&gt; methods. Lossless is the process in which all information will be preserved from the original file. Better compression can come form &lt;code&gt;lossy compression&lt;/code&gt; but we do not refer to them in detail here, but note that the preprocessing steps above are already a form of lossy compression. Lossy compression works very well when we have information that has low probability of being relevant to a search and therefore we exclude it when compressing.&lt;/p&gt;
&lt;p&gt;The two laws below dictate how to better understand the relationship between collections, tokens and indexes and why compression matters in a large index.&lt;/p&gt;
&lt;h4&gt;Heap's law&lt;/h4&gt;
&lt;p&gt;Heap's law captures the relationship between collection size and vocabulary sizes and states that this relationship is linear in log-log space. It dictates that a way to get the index vocabulary size &lt;code&gt;M&lt;/code&gt; is through &lt;code&gt;M=kT^b&lt;/code&gt; where &lt;code&gt;T&lt;/code&gt; is the the number o f tokens in the collection. &lt;code&gt;T&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt; are &lt;code&gt;30&amp;lt;=k&amp;lt;=100&lt;/code&gt; and &lt;code&gt;b ~ 0.5&lt;/code&gt;. The &lt;code&gt;k&lt;/code&gt; parameter can vary a lot due to the nature of the collection. Pre-processing steps like case folding and stemming reduce the growth rate, while including spelling errors increases it. In any way, Heap's law states that:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The vocabulary size will increase ad the collection size increases&lt;/li&gt;
&lt;li&gt;The size of dictionary is large for large collections.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Both of these hypothesis reinforce the importance of compressing an index.&lt;/p&gt;
&lt;h4&gt;Zipf's law&lt;/h4&gt;
&lt;p&gt;Zipf's law dictates how documents are distributed across documents. It states that if &lt;code&gt;t1&lt;/code&gt;, &lt;code&gt;t2&lt;/code&gt;, &lt;code&gt;t3&lt;/code&gt;... &lt;code&gt;ti&lt;/code&gt; are the most common terms then the collection frequency &lt;code&gt;cf-i&lt;/code&gt; of the &lt;em&gt;i-th&lt;/em&gt; most common element is proportional to &lt;code&gt;1/i&lt;/code&gt;. This means that most frequent terms occur &lt;code&gt;cf-1&lt;/code&gt; times, then the second term half as many times, then the third, a third as many times.&lt;/p&gt;
&lt;h3&gt;Dictionary compression&lt;/h3&gt;
&lt;p&gt;Now we will see different dictionary data structures that achieve high compression ratios. Compared to a postings file, a dictionary is usually smaller. We compress dictionaries to ensure they are available in memory and there's no need to have them in disk. This is because the more disk reads - the slower our search query. In essence, we always want to conserve memory and have start up time in our information retrieval systems.&lt;/p&gt;
&lt;p&gt;The simplest data structure for a dictionary is using strings and sort the vocabulary lexicographically. We can store the dictionary in an array of string entries allocating ~20 bytes for each term, ~4 bytes for document frequency and ~4 bytes for the pointer to its postings list. In other words, we replace long terms with short identifiers which are more compact to encode and to handle.&lt;/p&gt;
&lt;h3&gt;Postings file compression&lt;/h3&gt;
&lt;p&gt;Another way of compressing data is compressing the postings file directly. We can use a different and more efficient representation of the posting file that uses fewer than 20 bits per document and sort items per frequency. The idea of this approach is ensuring that the gaps between finding documents are the shortest they can be - frequent terms will have small gaps, while more disperse terms will have a fixed, predictable size. To ensure we can do this, we need to use &lt;code&gt;variable encoding&lt;/code&gt; using bytewise compression and bitwise compression.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;Variable byte encoding&lt;/code&gt; uses an integral number of bytes to encode a gap. The last 7 bits of the byte are used to encode part of the gap, the first bit is a continuation. Then the search code will know where terms stop or start. This process has a lot more detail in the &lt;a href="https://nlp.stanford.edu/IR-book/html/htmledition/variable-byte-codes-1.html"&gt;Variable byte code&lt;/a&gt; section. This section goes into detail on why VB encoding is a god tradeoff between time and space.&lt;/p&gt;
&lt;p&gt;If disk space is a scare resource we can use better compression ratios by using bit-level encodings with &lt;code&gt;gamma&lt;/code&gt; and &lt;code&gt;delta&lt;/code&gt; codes.&lt;/p&gt;</description><category>compression</category><category>indexing</category><category>information retrieval</category><category>textbook</category><guid>posts/notes-on-information-retrieval-5/</guid><pubDate>Sun, 29 Oct 2023 20:56:00 GMT</pubDate></item><item><title>#7 Building an inverted indexes and other types of indexes</title><link>posts/notes-on-information-retrieval-4/</link><dc:creator>Sebastian Montero</dc:creator><description>&lt;blockquote&gt;
&lt;p&gt;This is a blog series on Information Retrieval covering the first chapters of the textbook &lt;a href="https://nlp.stanford.edu/IR-book/information-retrieval-book.html"&gt;Introduction to Information Retrieval&lt;/a&gt;. This post covers &lt;strong&gt;Chapter 4: Index construction&lt;/strong&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Now we will look into how to construct an inverted index from scratch. The process that indexes data is the indexer and the design of the algorithm will depend on the hardware that is available.&lt;/p&gt;
&lt;p&gt;The system you want to build will heavily depend on the hardware you have available and the problems you are trying to solve. Some key points that are important for any system include:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Access to in-memory data is way faster to accessing data on disk - we want to keep as much data as possible in memory. We call this &lt;code&gt;caching&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;The time it takes during the disk head to move to the part where the data is stored is called &lt;code&gt;seek time&lt;/code&gt; and it usually is 5ms - at this point no data is transferred. To minimize the seek time, similar data must be stored together&lt;/li&gt;
&lt;li&gt;I/O operations happen in blocks so reading a single byte will take the same time as the whole block.&lt;/li&gt;
&lt;li&gt;Data transfers are not handled by the processor so it can be used for other tasks&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;We will look into 4 ways of building indexes.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Blocked sort-based indexing (in-memory indexing for static collections)&lt;/li&gt;
&lt;li&gt;Single-pass in-memory indexing (indexing for static collections where vocab is in disk)&lt;/li&gt;
&lt;li&gt;Distributed Indexing&lt;/li&gt;
&lt;li&gt;Dynamic indexing (collections with frequent changes)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Blocked sort-based indexing&lt;/h3&gt;
&lt;p&gt;Recall that for small collections we usually create a pair of &lt;code&gt;term - docID&lt;/code&gt; and organize the docIDs into postings list to compute statistics. For large collection we need to use secondary storage. To make construction faster, we represent terms as termIDs instead of term strings. We can process the mapping as we build the collection or in a two-pass approach.&lt;/p&gt;
&lt;p&gt;If main memory is insufficient we can use an &lt;code&gt;external sorting algorithm&lt;/code&gt; such as the &lt;code&gt;blocked sort-based indexing algorithm (BSBI)&lt;/code&gt; that uses disk. BSBI breaks the collection into equal sizes, sorts the termID-docID pairs in memory and then stored the intermediate results in disk. Finally it merges all intermediate results into the final index. The algorithm will parse documents in memory until the block is full. BSBI's time complexity is defined by the time it takes to parse the documents and to do the final merge.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Single-pass in-memory indexing&lt;/h3&gt;
&lt;p&gt;Given that BSBI still required a data structure to access the mappings, it is not suitable for very large collections. An alternative is &lt;code&gt;single-pass in-memory indexing (SPIMI)&lt;/code&gt;. SPIMI writes new dictionaries for each block into disk so its limit is non memory-based but disk-based. SPIMI adds postings directly into its postings list - it doesn't collect all and then sorts them. The advantage of this process is that it is faster because it avoids sorting and saves memory. For SPIMI it is normal to double the memory space each time a postings list is full. When memory is exhausted then it is written to disk. SPIMI usually relies on compression as well to improve memory savings and speed.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Distributed indexing&lt;/h3&gt;
&lt;p&gt;When collection are so large we can't perform indexing in a single machine. This is usually the case for search engines which require computer clusters. This type of index ins partitions over multiple computers. The index is usually partitioned according to a term or document - this is called a &lt;code&gt;term-partitioned index&lt;/code&gt; or a &lt;code&gt;document-partitioned index&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The index construction method is an application of the &lt;code&gt;MapReduce&lt;/code&gt; algorithm used in distributed computing. Given that this process is used in multiple computer nodes we expect the hardware to be cheap and machines to be replaceable if one fails. The process relies on one or several &lt;code&gt;master nodes&lt;/code&gt; to ensure it works properly.&lt;/p&gt;
&lt;p&gt;The Map and Reduce phases split the computing job into chunks that can be processed in a short time. In this case the termId-docId pairs are distributed across multiple machines making the process more complex. To solve this we can keep a mapping of frequent terms mapped to different nodes.&lt;/p&gt;
&lt;p&gt;During the &lt;code&gt;map&lt;/code&gt; phase the data is split into multiple key-value pairs - such as the last two indexing methods. During the &lt;code&gt;reduce&lt;/code&gt; phase we reshuffle the values so values for a given key are stored nearby and can be processed quickly on downstream tasks.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Dynamic indexing&lt;/h3&gt;
&lt;p&gt;Dynamic indexing comes in when the document collection changes over time and is not static. The previous examples work under the assumption that the document collection is static which work for collections that change infrequently or never. The fastest way to create a dynamic index is to periodically reconstruct the whole index from scratch if the changes over time are small and if the final use case accepts that limitation.&lt;/p&gt;
&lt;p&gt;An approach to having a dynamic index is to use an &lt;code&gt;auxiliary index&lt;/code&gt; next to your main index. Queries are run on both indexes are results are merged. When the auxiliary index becomes too large we can merge it into the main index. The best way to merge indexes it to extend the postings list, however this is not always feasible for the end user.&lt;/p&gt;</description><category>indexing</category><category>information retrieval</category><category>textbook</category><guid>posts/notes-on-information-retrieval-4/</guid><pubDate>Sat, 29 Jul 2023 20:56:00 GMT</pubDate></item></channel></rss>