<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet type="text/xsl" href="../assets/xml/rss.xsl" media="all"?><rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>SM (Posts about information retrieval)</title><link></link><description></description><atom:link href="categories/information-retrieval.xml" rel="self" type="application/rss+xml"></atom:link><language>en</language><copyright>Contents © 2023 &lt;a href="mailto:sebastianmont@duck.com"&gt;Sebastian Montero&lt;/a&gt; </copyright><lastBuildDate>Tue, 21 Mar 2023 00:33:52 GMT</lastBuildDate><generator>Nikola (getnikola.com)</generator><docs>http://blogs.law.harvard.edu/tech/rss</docs><item><title>Ensuring tolerant retrieval in your search system</title><link>posts/notes-on-information-retrieval-3/</link><dc:creator>Sebastian Montero</dc:creator><description>&lt;blockquote&gt;
&lt;p&gt;This is a blog series on Information Retrieval covering the first chapters of the textbook &lt;a href="https://nlp.stanford.edu/IR-book/information-retrieval-book.html"&gt;Introduction to Information Retrieval&lt;/a&gt;. This post covers &lt;strong&gt;Chapter 3: Dictionaries and tolerant retrieval&lt;/strong&gt;. The first section of the chapter is in its separate blog: &lt;a href="posts/notes-on-information-retrieval-3-dictionaries/"&gt;How is a dictionary data structure optimized for search?&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Tolerant retrieval is an essential for users querying an information retrieval system. Tolerance at query time includes fixing user mistakes such as spelling mistakes, alternative spelling, phonetic corrections and also includes the flexibility for users to use wildcards during their search.&lt;/p&gt;
&lt;p&gt;This section will introduce wildcards and query correction.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Searching with wildcards&lt;/h3&gt;
&lt;p&gt;Many search systems enable searching with wildcards, so it has become a standard in modern systems. There are many reasons why a user might want to use wildcards. These include:
1. The user is unsure of the right spelling for a term
2. The user knows of various ways to spell a word and wants an exhaustive result (e.g. &lt;code&gt;col*r&lt;/code&gt; for &lt;code&gt;color&lt;/code&gt; and &lt;code&gt;colour&lt;/code&gt;)
3. The user wants to capture the stem of the word (e.g. &lt;code&gt;fl*&lt;/code&gt; for &lt;code&gt;fly&lt;/code&gt; and &lt;code&gt;flew&lt;/code&gt;)
4. The user is unsure of a foreign word or phrase&lt;/p&gt;
&lt;p&gt;In this section we will introduce are different types of wildcard queries. A query where the wildcard symbol &lt;code&gt;*&lt;/code&gt; (e.g. &lt;code&gt;eng*&lt;/code&gt;) occurs at the end of the term is known as a &lt;strong&gt;trailing wildcard query&lt;/strong&gt;. This type of query can easily be handled by search tree dictionaries - specifically B-trees. With a B-tree, we can traverse the dictionary until we get to the final letter and then get all the terms under that final letter. All of the documents matching these terms are then retrieved from our inverted index. We can also have a &lt;strong&gt;leading wildcard query&lt;/strong&gt; where the wildcard symbol &lt;code&gt;*&lt;/code&gt; is at the beginning fo the term. For these types of queries we can consider using a &lt;strong&gt;reverse B-tree&lt;/strong&gt; as our dictionary, where the root to leaf paths are backwards. &lt;/p&gt;
&lt;p&gt;Using both types of B-trees we can handle wildcard queries in the middle of a word such as &lt;code&gt;col*r&lt;/code&gt;. In this case we can use the regular B-tree to find terms starting with &lt;code&gt;col&lt;/code&gt; and then the reverse B-tree to find the terms ending in &lt;code&gt;r&lt;/code&gt;. After we traverse both search trees, we can intersect both sets of results to find terms that start with &lt;code&gt;col&lt;/code&gt; and end with &lt;code&gt;r&lt;/code&gt;. From here we can again use our inverse index to retrieve target documents with these terms.&lt;/p&gt;
&lt;p&gt;Now we will introduce two additional wildcard query search algorithms that express the wildcard query as a boolean query on a &lt;em&gt;special wildcard index&lt;/em&gt;. This specialized index considers the boolean query as a superset of terms that match the wildcard index. After we retrieve the terms from the wildcard index, each term is filtered down to the terms matching our query. &lt;/p&gt;
&lt;h4&gt;Permuterm indexes&lt;/h4&gt;
&lt;p&gt;First, we introduce the &lt;strong&gt;permuterm index&lt;/strong&gt;. This index uses a special character to match the end of each term that will be indexed (for example &lt;code&gt;hello&lt;/code&gt; becomes &lt;code&gt;hello$&lt;/code&gt;). Then we build the permuterm index by rotating the term and map it to the original term. So a set of &lt;code&gt;(hello$, ello$h, llo$he, lo$hel, o$hell)&lt;/code&gt; all map to &lt;code&gt;hello&lt;/code&gt;. The rotated superset of terms is called the &lt;strong&gt;permuterm vocabulary&lt;/strong&gt;. This query helps in faster searching because it allows us to rotate the original term with a wildcard to search for the desired term permutations. If we want to search for &lt;code&gt;he*o&lt;/code&gt;, we then transform the term so the wildcard symbol is at the end so &lt;code&gt;he*o&lt;/code&gt; becomes &lt;code&gt;o$he*&lt;/code&gt;. We then look for the &lt;code&gt;o$he&lt;/code&gt; term in the permutation index as a leading wildcard query. A permuterm index uses a B-tree. It is important to note that this type of index could get large very quick. &lt;/p&gt;
&lt;h4&gt;k-gram indexes&lt;/h4&gt;
&lt;p&gt;The next type of index is a &lt;strong&gt;k-gram index&lt;/strong&gt;. To build this specialised wildcard index, we need to build a set of k-grams for each term. a &lt;strong&gt;k-gram&lt;/strong&gt; is a sequence of &lt;code&gt;k&lt;/code&gt; characters that are adjacent in a term. We again use a special symbol such as &lt;code&gt;$&lt;/code&gt; to denote the beginning and the end of the term. So in the case of the word &lt;code&gt;hello&lt;/code&gt;, a 3-gram index would include the words &lt;code&gt;($he, hel, ell, llo, lo$)&lt;/code&gt;. Then we can index each of these k-grams in our k-gram index. When we search for &lt;code&gt;he*o&lt;/code&gt;, we then search for the k-grams that make up this phrase. K-grams sometimes require post-filtering steps that check the original query against the returns terms to filter out terms that don't match. &lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Spelling errors, alternate spellings and phonetic corrections&lt;/h3&gt;
&lt;p&gt;When it comes to spelling corrections and alternate spellings, there are two principles that need to be followed: First, the system should always chose the nearest term based on a number of alternative correct spellings. This requires that our system has an understanding of term proximity. Second, if you have more than one match to a correct term, select the most common one. You can do this by looking at the occurrences of the term in the collection.&lt;/p&gt;
&lt;p&gt;Before we will introduce spelling correction algorithms, we can discuss how we run these corrections. We can use &lt;strong&gt;isolated-term&lt;/strong&gt; correction, this is the type of correction that will identify a misspelled term and attempt to correct each term individually. Note that the system will not understand if the query as a whole makes sense, just its individual parts, hence the phrase &lt;code&gt;flew form Heathrow&lt;/code&gt; would be correct as each term is individually correct. On the other hand, we also have &lt;strong&gt;context-sensitive&lt;/strong&gt; correction which considers the search phrase as a whole. We will proceed to look into two algorithms for isolated-term corrections: edit distance and k-gram overlap.&lt;/p&gt;
&lt;p&gt;The &lt;strong&gt;edit distance&lt;/strong&gt; algorithm takes two strings &lt;code&gt;s1&lt;/code&gt; and &lt;code&gt;s2&lt;/code&gt; and defines the distance as the number of edit operations required to transform &lt;code&gt;s1&lt;/code&gt; to &lt;code&gt;s2&lt;/code&gt;. The edit operations can include: inserting a character, deleting a character, replacing a character. The edit distance can also be called the &lt;strong&gt;Levenshtein distance&lt;/strong&gt;. The edit distance can also be changed to allow for different weights for different operations. Setting weights for the likelihood of making a change or a mistake is very effective. I will not go into detail of the algorithm in this post; however this can be found in section 3.3.3 of the book.&lt;/p&gt;
&lt;p&gt;Another method for spelling correction using k-gram indexes is called &lt;strong&gt;k-gram overlap&lt;/strong&gt;. We can use k-gram indexes to limit the set of vocabulary terms for which we compute the edit distances. Using a k-gram index will results in many terms that have common k-grams. From here we will need to do a linear scan through the postings to filter for the k-grams that overlap with the search query.&lt;/p&gt;
&lt;p&gt;In a retrieval system, we might want to first look into isolated-term correction if it identifies an issue with the query, but if there are a small number of documents returned without a any correction or after isolated-term corrections, we might want the system to do context-sensitive correction. To do this we need to provide alternative spellings for all the terms being searched and try to substitute them in the query. This operation can be computationally expensive so we could use simple heuristics like looking at the most frequent alternative spelling only or look at bi-word statistics and find what words are usually used together.&lt;/p&gt;
&lt;p&gt;Another form of correction is the phonetic corrections. This type of correction requires a specialized index and which considers how terms sound. To build a phonetic search index, we need to create a &lt;code&gt;phonetic hash&lt;/code&gt; that map similar word sounds to the same hash. Phonetic hashing is collectively known as &lt;strong&gt;soundex&lt;/strong&gt; algorithms. At the most basic level a soundex algorithm will turn every indexable term into a hash form and build a soundex index from that, then do the same for query terms and finally use the soundex index to see if there is a match to the soundex query. Even though we are not going too much in the detail of soundex algorithms, they work very well, specially with names. A caveat of this indexes, however, is that they are alphabet dependent.&lt;/p&gt;</description><category>information retrieval</category><category>textbook</category><category>tolerant retrieval</category><guid>posts/notes-on-information-retrieval-3/</guid><pubDate>Tue, 21 Mar 2023 00:26:00 GMT</pubDate></item><item><title>How is a dictionary data structure optimized for search?</title><link>posts/notes-on-information-retrieval-3-dictionaries/</link><dc:creator>Sebastian Montero</dc:creator><description>&lt;blockquote&gt;
&lt;p&gt;This is a blog series on Information Retrieval covering the first chapters of the textbook &lt;a href="https://nlp.stanford.edu/IR-book/information-retrieval-book.html"&gt;Introduction to Information Retrieval&lt;/a&gt;. This post covers &lt;strong&gt;Chapter 3, Section 1: Search structures for dictionaries&lt;/strong&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;For any IR task we need to start with an inverted index and a user query. The search process is the following: First, the system finds the query terms in the index vocabulary and second it identifies pointers to the term's postings&lt;/p&gt;
&lt;p&gt;The vocabulary lookup relies on a dictionary data structure. For IR examples the vocabulary terms will be the dictionaries' keys. Dictionaries can categorized into hashing and search trees. To decide if we want to use a hashing dictionary or a search tree dictionary we need to define the number of keys, the frequency of updates to these keys, and how often keys will be accessed. &lt;/p&gt;
&lt;p&gt;In the hashing data structure, each key is transformed into an integer representation. At query time, we search for the specific integers and retrieve the postings. In this case, we can't make any decision on alternate spellings as the system only sees integers instead of the words themselves.&lt;/p&gt;
&lt;p&gt;Search trees, on the other hand, allow the system to use a term slice (e.g. &lt;code&gt;sta&lt;/code&gt; for &lt;code&gt;stack&lt;/code&gt; or &lt;code&gt;star&lt;/code&gt;) as a node in the tree. The most common search tree is a &lt;strong&gt;binary tree&lt;/strong&gt;, where the tree options are split in two. When an algorithm makes it way through the tree is performs logical operations to find the right path. The issue here is that a binary tree needs to be balanced (trees under a single node is equal or differs by one) and when the structure changes, rebalancing becomes an expensive operation. To avoid rebalancing the number of paths can be flexible. Another type of search tree is a &lt;strong&gt;B-tree&lt;/strong&gt;. Every node in the B-tree has a number of child nodes in a specific interval, which allows to collapse various levels of the binary tree. This is an advantage as it allows the dictionary to be optimized for being stored in disk, depending on the size of the intervals.&lt;/p&gt;
&lt;figure&gt;
  &lt;img src="images/binary_search_tree.png" alt="Binary Search Tree (From Wikipedia)" width="25%"&gt;
  &lt;figcaption&gt;Binary Search Tree (&lt;a href="https://en.wikipedia.org/wiki/Binary_search_tree"&gt;Wikipedia&lt;/a&gt;)&lt;/figcaption&gt;
&lt;/figure&gt;

&lt;p&gt;Unlike hashing, search trees require characters in the document collection to be ordered. This works for the English language but some issues might arise in other languages.&lt;/p&gt;</description><category>dictionary</category><category>information retrieval</category><category>search</category><category>textbook</category><guid>posts/notes-on-information-retrieval-3-dictionaries/</guid><pubDate>Thu, 16 Mar 2023 23:38:28 GMT</pubDate></item><item><title>How to process index tokens and optimize for fast information retrieval</title><link>posts/notes-on-information-retrieval-2/</link><dc:creator>Sebastian Montero</dc:creator><description>&lt;blockquote&gt;
&lt;p&gt;This is a blog series on Information Retrieval covering the first chapters of the textbook &lt;a href="https://nlp.stanford.edu/IR-book/information-retrieval-book.html"&gt;Introduction to Information Retrieval&lt;/a&gt;. This post covers &lt;strong&gt;Chapter 2: The term vocabulary and postings lists&lt;/strong&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;In the previous blog we talked about how to create an inverted index. In this second part, we will look into the intricacies of how a &lt;em&gt;token&lt;/em&gt; can be defined and standardized in order to add it into an index as a &lt;em&gt;term&lt;/em&gt;, and how to use optimize a postings list data structure to ensure faster retrieval.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Indexing terms&lt;/h3&gt;
&lt;p&gt;In very low-level computing terms, every documents must be read into memory as characters and words (see Note 1) with a particular sequence through a decoding algorithm. Depending on the language, there will be different cases on how files are decoded into a sequence of characters and how that language is read. For example, Arabic languages are written from right to left and can include numbers written from left to right. For this section we will work with the Latin alphabet where characters, and more specifically the English language, where words and numbers are written sequentially from left to right. &lt;/p&gt;
&lt;p&gt;Once we have a sequence of characters, we can start the task of defining the &lt;strong&gt;document unit&lt;/strong&gt; that will be used for indexing. There are some intricacies on how to define &lt;em&gt;what&lt;/em&gt; a document unit and a document itself is. For example, emails can contain large threads and attachments - are these all part of the same document? On the other hand, what happens with long documents? Do we take the &lt;em&gt;entire&lt;/em&gt; file as our &lt;em&gt;document&lt;/em&gt; or do we break it into smaller pieces? Depending on the &lt;strong&gt;granularity&lt;/strong&gt; we want in our information retrieval system, there will always be a precision - recall trade off: small documents will be low on precision (many documents might come up), but high on recall and larger documents will be high on recall and low on precision (information will be hard to find).&lt;/p&gt;
&lt;h4&gt;Using preprocessing to transform sequences into index-ready terms&lt;/h4&gt;
&lt;p&gt;After defining our the document unit and decoding our text data into a sequence of characters, we can start working towards creating our &lt;strong&gt;tokens&lt;/strong&gt; by breaking down our sequence at specific intervals. In the case of a sentence, tokens can be the words that make up that sentence split by white space. It is important to note that tokens can also be defined as terms or words; however we define tokens as instances that can be grouped together under the same token &lt;strong&gt;type&lt;/strong&gt;. The &lt;strong&gt;terms&lt;/strong&gt;, on the other hand, are the types that are included in an information retrieval system dictionary, these are related to tokens but include a few preprocessing steps in between, which we will discuss next.&lt;/p&gt;
&lt;p&gt;There will always be a challenge defining how to create a tokens. Some words are very straight forward to index, but others take more critical thining. For example, take the word &lt;code&gt;don't&lt;/code&gt; - shall we tokenize it as &lt;code&gt;don't&lt;/code&gt;, &lt;code&gt;dont&lt;/code&gt;, &lt;code&gt;don&lt;/code&gt; and &lt;code&gt;t&lt;/code&gt;, or &lt;code&gt;do&lt;/code&gt; and &lt;code&gt;n't&lt;/code&gt;? Most of the issues in defining how to tokenize will be language specific so identifying a language before tokenizing is useful to catch and process these language-specific patterns. Whatever strategy we choose we need to ensure that we apply the same strategy at the user retrieval side to ensure the same processing is done and the term types are the same.&lt;/p&gt;
&lt;p&gt;Some other issues with tokenization in the English language include:
- How should we tokenize sequences with "special" characters such as proper nouns, urls, monetary values, etc? (e.g. &lt;code&gt;Turkey&lt;/code&gt; (the country), &lt;code&gt;https://www.google.com/&lt;/code&gt;, &lt;code&gt;$100&lt;/code&gt;)
- Should we separate on hyphens (-), when hyphens can be used for naming or grouping words? 
- Should we separate on spaces as some sequences don't can mean the wrong thing independently e.g. &lt;code&gt;New York&lt;/code&gt;, &lt;code&gt;whitespace&lt;/code&gt; vs &lt;code&gt;white space&lt;/code&gt;, dates, etc.&lt;/p&gt;
&lt;p&gt;After deciding our tokenization strategy, we can start filtering for common words that do not add value to our searches. These words are called &lt;strong&gt;stop words&lt;/strong&gt;, the set of stop words is called a &lt;strong&gt;stop list&lt;/strong&gt;. Commonly we can group terms by frequency across all collections (&lt;strong&gt;collection frequency&lt;/strong&gt;) and filter them our based on a human-in-the-loop assessment, assuming that common words across all collections don't add a lot of information value to any single document. It is important to consider that we should &lt;em&gt;not always&lt;/em&gt; discard these stop words as they can be valuable in some phrases (e.g. the song &lt;code&gt;Let It Be&lt;/code&gt;); however, phrase processing will be discussed later in this blog. Recent trends have also started to keep the stop word list in favour of more robust language processing for indexing and content understanding.&lt;/p&gt;
&lt;p&gt;After processing stop words, we can start normalizing our tokens. We do this because there are tokens that mean the same thing but are represented differently such as &lt;code&gt;UK&lt;/code&gt;, &lt;code&gt;U.K.&lt;/code&gt;, and &lt;code&gt;United Kingdom&lt;/code&gt;. &lt;strong&gt;Normalization&lt;/strong&gt; is the process of ensuring all our terms are represented equally. The most standard way is to manually create &lt;strong&gt;equivalence classes&lt;/strong&gt;, which are dictionaries of "ground truth" terms mapped to different spellings. An alternative is to maintain both normalized and unnormalized terms and use them by keeping an index of unnormalized terms and a dictionary to expand the search query into all unnormalized terms. &lt;/p&gt;
&lt;p&gt;There are other processing techniques that can process the differences in how tokens are represented grammatically in language. These are stemming and lemmatization. &lt;strong&gt;Stemming&lt;/strong&gt; refers to the process of slicing the ends of the words to remove grammatical differences (e.g. &lt;code&gt;colored&lt;/code&gt; and &lt;code&gt;colors&lt;/code&gt; become &lt;code&gt;color&lt;/code&gt;). Stemming in the English language most commonly relies on &lt;code&gt;Porter's algorithm&lt;/code&gt; which reduces word sequences in 5 stages. This blog will not go into the details, but these can be found in &lt;a href="http://www.tartarus.org/~martin/PorterStemmer/"&gt;the Porter Stemmer official site&lt;/a&gt;. On the other hand, &lt;strong&gt;lemmatization&lt;/strong&gt; tries to use morphological analysis to analyze the words and transform them into their base dictionary form using techniques from Natural Language Processing (e.g. &lt;code&gt;saw&lt;/code&gt;, &lt;code&gt;sees&lt;/code&gt; and &lt;code&gt;seeing&lt;/code&gt; become &lt;code&gt;see&lt;/code&gt;). This base form is called the &lt;em&gt;lemma&lt;/em&gt;. Even though stemming and lemmatization can help in normalizing how we represent tokens, they usually come with marginal gains and other tradeoffs.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Updates to the postings list&lt;/h3&gt;
&lt;p&gt;Just how preprocessing tokens into terms can help in increasing the precision of our information retrieval system, we can also optimize the postings list data structures to make the retrieval computation more efficient and accurate. &lt;/p&gt;
&lt;h4&gt;Skip pointers in postings lists&lt;/h4&gt;
&lt;p&gt;If we want to analyze to two lists of sizes &lt;code&gt;m&lt;/code&gt; and &lt;code&gt;n&lt;/code&gt; simultaneously, the time complexity will be &lt;code&gt;O(m+n)&lt;/code&gt;. To reduce the time complexity below this linear form, we can use a &lt;strong&gt;skip list&lt;/strong&gt; - this is a change to the underlying list data structure that contains &lt;strong&gt;skip pointers&lt;/strong&gt;. Skip pointers allow us to skip parts of the list that will not appear in the search result. The general idea is that when we step through multiple lists, we can skip to a matched document in another list allowing the document retrieval to be done faster. For example, if we take two posting lists &lt;code&gt;A: {2,3,4,5,6,7}&lt;/code&gt; and &lt;code&gt;B:{1,4,5,7}&lt;/code&gt; the moment we match on the document with index &lt;code&gt;4&lt;/code&gt;, we can skip to the next pointer in both lists.&lt;/p&gt;
&lt;h4&gt;Processing phrase queries&lt;/h4&gt;
&lt;p&gt;It is common that users want to use &lt;strong&gt;phrase queries&lt;/strong&gt; such as &lt;code&gt;New York&lt;/code&gt; and retrieve messages that are familiar with the city and not the words &lt;code&gt;new&lt;/code&gt; and &lt;code&gt;york&lt;/code&gt;. Because of this, the reader might be familiar with the use of double quotes (&lt;code&gt;" "&lt;/code&gt;) around a token to get an exact match. To support these types of phrase queries using exact or close matches, a search system needs to support and properly implement phrase indexes or use proximity indexes that weight terms that appear closer in different documents.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Biword indexes&lt;/strong&gt; or &lt;strong&gt;phrase index&lt;/strong&gt; are a type of index that considers words that are together in the document (data practitioners familiar with NLP might relate this to n-grams). Adding these terms into our indexes allows for immediate search amongst them. A downside to this approach is that there might be a large number of false positives. To avoid these we can focus on &lt;em&gt;noun phrases&lt;/em&gt; and some preprocessing. For example if we take a noun phrase such as &lt;code&gt;renegotiation of the constitution&lt;/code&gt; we can perform tokenization and part-of-speech tagging to extract nouns from the sub-tokens and this phrase can become the term &lt;code&gt;renegotiate constitution&lt;/code&gt;. A phrase index is a good solution but there can be issues resulting in multiple false positives if we combine a phrase index with single word indexes (which is usually required). &lt;/p&gt;
&lt;p&gt;For the issues described above, phrase indexes are not the standard solution. Instead, &lt;strong&gt;positional indexes&lt;/strong&gt; are most commonly used for IR. To create a positional index, we need to store postings in the form of &lt;code&gt;docID: {position1, position2, ...}&lt;/code&gt; where each position is the position of the token in the document with the number of times it occurs (expanded into &lt;code&gt;term, totalFrequency: {docId, frequency:{position1, position2}}&lt;/code&gt;). The retrieval algorithm for phrases will look for documents with both terms and then find documents where the position of the second term is one higher than the first. This retrieval can also be used with &lt;code&gt;k&lt;/code&gt; positions. Additional notes on sizes of the positional index in Notes 2.&lt;/p&gt;
&lt;p&gt;Finally, we can also combine the strategies of phrase indexes and positional indexes. This is done by using phrase indexes for certain queries and positional indexes for other queries. The normal approach is to add common queries in the positional index into the phrase index to ensure faster retrieval.&lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Notes&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;On document decoding; At the very basic level, every term is a byte in a file. These bytes are decoded into human-readable characters. Usually the document can be decoded using the encoding scheme found in the file's metadata so an in-depth analysis of byte decoding and encoding will are spared in this post. More details on this can be found in the book. &lt;/li&gt;
&lt;li&gt;On positional index size; Using positional indexes will increase the size of the stored index as well as the computing complexity. &lt;/li&gt;
&lt;/ol&gt;</description><category>index terms</category><category>information retrieval</category><category>postings list</category><category>textbook</category><category>tokens</category><guid>posts/notes-on-information-retrieval-2/</guid><pubDate>Thu, 16 Mar 2023 17:18:00 GMT</pubDate></item><item><title>Introducing boolean retrieval and the inverse index</title><link>posts/notes-on-information-retrieval-1/</link><dc:creator>Sebastian Montero</dc:creator><description>&lt;blockquote&gt;
&lt;p&gt;This is a blog series on Information Retrieval covering the first chapters of the textbook &lt;a href="https://nlp.stanford.edu/IR-book/information-retrieval-book.html"&gt;Introduction to Information Retrieval&lt;/a&gt;. This post covers &lt;strong&gt;Chapter 1: Boolean retrieval&lt;/strong&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;What is Information Retrieval?&lt;/h3&gt;
&lt;p&gt;Information Retrieval (IR) is all about finding material that satisfies the information need of a user. Often this material is in the form on unstructured text data, such as in the typical case of online searches, where IR is most commonly used. Other applications also include email search, computer search, knowledge databases, etc. In the past, most companies used to rely on structured data for search; however around 1990s, the production of unstructured data increased exponentially, giving a raise to advances in information retrieval over large quantities of content, and quickly replacing traditional database searching where data pre-processing of such content was required.&lt;/p&gt;
&lt;p&gt;The IR field also includes additional tasks such as searching on semi-structured data (e.g. code bases where you want to find a specific HTML tag within a CSS file), and grouping/clustering documents together to help users in surfacing documents faster (and satisfying their information needs) by understanding their topics during browsing and filtering relevant results.&lt;/p&gt;
&lt;p&gt;Depending on the volume of data, IR systems can be divided into three main scales:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Web search: search over billions of documents. This scale has challenges such as document gathering and indexing, system scalability and handling web-specific attributes like hypertext. &lt;/li&gt;
&lt;li&gt;Enterprise / domain specific: Can include internal documents within a company or domain-specific searches like research papers, company search tools, patent search tools, etc. Usually these documents are in a centralized location. &lt;/li&gt;
&lt;li&gt;Personal: These can be part of personal computers, email apps, etc. They include user-specific features like categorizing emails into spam. The main challenge here is handling different data types and making the system maintenance tools lightweight.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;Baby steps: a simple boolean retrieval example&lt;/h3&gt;
&lt;p&gt;Take the book &lt;em&gt;Shakespeare's Collected Works&lt;/em&gt; which includes multiple plays by Shakespeare as our search collection. In this example, a user wants to find plays containing the characters &lt;code&gt;BRUTUS AND CESAR and NOT CALPURINA&lt;/code&gt;. This is a typical IR problem where you are filtering for documents that contain similar terms and also excluding additional terms. Even though a computer can easily do a GREP (linear scan) through the documents on this collection (which includes just a few million words), we might want to approach this problem as an information retrieval solution which allows us to:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Search a larger collection quicker that using GREP&lt;/li&gt;
&lt;li&gt;Allows for flexible operations when searching&lt;/li&gt;
&lt;li&gt;Allows for ranked retrieval&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To avoid linear scanning of text we need to create an &lt;strong&gt;index&lt;/strong&gt; of documents. In the &lt;em&gt;Shakespeare's Collected Works&lt;/em&gt; example, we could process each document (or play) and mark all the words that the documents contain. This results in an &lt;strong&gt;incidence matrix&lt;/strong&gt; (below) that contains the words or &lt;strong&gt;terms&lt;/strong&gt; being indexed. With our incidence matrix, we can filter for rows or columns containing the terms we are interested in.&lt;/p&gt;
&lt;p&gt;In the example using &lt;em&gt;Shakespeare's Collected Works&lt;/em&gt;, if we want to find &lt;code&gt;BRUTUS AND CESAR AND NOT CALPURNIA&lt;/code&gt;, we can run a logical operation in our index using boolean expressions: &lt;code&gt;1101 AND 1101 AND NOT 0100 = 1101 AND 1101 AND 1011 = 1001&lt;/code&gt;. The result is &lt;code&gt;1001&lt;/code&gt;, which mark the documents that satisfy our search query. Note that since the term &lt;code&gt;CALPURNIA&lt;/code&gt; is negated we are using the inverse of the row where &lt;code&gt;CALPURNIA&lt;/code&gt; appears. The table below  shows this example.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Anthony and Cleopatra&lt;/th&gt;
&lt;th&gt;Julius Caesar&lt;/th&gt;
&lt;th&gt;The Tempest&lt;/th&gt;
&lt;th&gt;Hamlet&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;BRUTUS&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CESAR&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CALPURNIA&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;RESULT&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;This is an example of a &lt;strong&gt;Boolean retrieval model&lt;/strong&gt; in which we search using boolean logic in the terms of &lt;code&gt;AND&lt;/code&gt;, &lt;code&gt;OR&lt;/code&gt; and &lt;code&gt;NOT&lt;/code&gt;. Note that in this case we are also marking the terms and documents as &lt;code&gt;0&lt;/code&gt; or &lt;code&gt;1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;&lt;br&gt;&lt;/p&gt;
&lt;h3&gt;What happens when we have way more documents? Enter the inverse index&lt;/h3&gt;
&lt;p&gt;Now let's say we have 1m &lt;strong&gt;documents&lt;/strong&gt; (meaning any unit we build our system on e.g. music, books, plays, patents, company information, etc.). These documents live in our &lt;strong&gt;collection&lt;/strong&gt; or &lt;strong&gt;corpus&lt;/strong&gt; (group of documents). &lt;/p&gt;
&lt;p&gt;Our goal is to allow the user to do ad hoc searches for &lt;strong&gt;ad hoc retrieval&lt;/strong&gt;. This means that we want to create a system where the can run multiple search queries and we can satisfy these. The user will provide a &lt;strong&gt;query&lt;/strong&gt; (which is an interpretation of the user's information needs that the computer can read) that can satisfy their &lt;strong&gt;information need&lt;/strong&gt; and have in return a set of documents that are &lt;strong&gt;relevant&lt;/strong&gt; to that information need. Relevance is measured by ensuring that the documents that are returned have value to the user for that specific query and information need. &lt;/p&gt;
&lt;p&gt;To ensure that the results of a search system are &lt;strong&gt;effective&lt;/strong&gt; we can use the two following metrics:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Precision&lt;/strong&gt;: Fraction of documents that are relevant to the user. (How many of the retrieved documents are relevant to the user?)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Recall&lt;/strong&gt;: Fraction of relevant documents that are retrieved. (How many documents are actually retrieved from the collection?)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;If we try to apply the boolean retrieval example of the previous section to a problem with ~1m documents where each document has ~500k terms,, we will have a matrix with ~500,000,000,000 1s and 0s, which will most likely cause a memory problem. If we have a large number of documents containing a large number of tokens, boolean retrieval is not the best approach to use, even more so considering that there will be a disproportionate amount of 0s to 1s (this is usually referred to as a sparse matrix).&lt;/p&gt;
&lt;p&gt;This issue can be solved by creating an &lt;strong&gt;inverted index&lt;/strong&gt;. The inverted index allows us to keep a &lt;strong&gt;dictionary&lt;/strong&gt; (or &lt;strong&gt;vocabulary&lt;/strong&gt;, &lt;strong&gt;lexicon&lt;/strong&gt;) of terms per document. We will build a &lt;strong&gt;postings list&lt;/strong&gt; that contains multiple &lt;strong&gt;postings&lt;/strong&gt;. A posting contains each document where a term appears. Take the table below as an example. More advanced search strategies can implement the &lt;strong&gt;term frequency&lt;/strong&gt; within the inverted index to have higher weights on documents being retrieved.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Term&lt;/th&gt;
&lt;th&gt;Document number&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;BRUTUS&lt;/td&gt;
&lt;td&gt;2, 3, 5&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CESAR&lt;/td&gt;
&lt;td&gt;2, 3, 6&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CALPURNIA&lt;/td&gt;
&lt;td&gt;1, 2, 3&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;We always need to build an inverted index in advance by collecting the documents that we are interesting in searching and then tokenizing the text (this means separating each document into unique words) The results are a list of tokens per document where we can then do natural language preprocessing standardise the terms before finally indexing the terms per documents. Standardisation ensures that examples like &lt;code&gt;searched&lt;/code&gt; and &lt;code&gt;searching&lt;/code&gt; can be traced back to the same underlying token &lt;code&gt;search&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;We also need to ensure that each document has a unique document identifier or &lt;strong&gt;DocID&lt;/strong&gt; that will be used to refer back to that document. We can also ensure that terms are sorted alphabetically and the term to DocID pairs are unique helping in speeding up computation and reducing the memory footprint of the index.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;This inverted index structure is essentially without rivals as the most efficient structure for supporting ad hoc text search.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Now, how do we go from a boolean query into an inverted index search query? To do this we need to follow a process called &lt;strong&gt;simple conjunctive querying&lt;/strong&gt;. If we take the example of &lt;code&gt;BRUTUS AND CALPURNIA&lt;/code&gt; we need to locate both terms in the inverted index and retrieve their postings, then &lt;strong&gt;intersect&lt;/strong&gt; or &lt;strong&gt;merge&lt;/strong&gt; both sets of postings. We can also perform &lt;strong&gt;query optimization&lt;/strong&gt; such as query ordering, which transforms the input query to ensure the least amount of work is done by the system.&lt;/p&gt;
&lt;p&gt;Recently more advanced techniques such as vector space models have become popular because they enable &lt;strong&gt;ranked retrieval models&lt;/strong&gt;. The vector space model allows the user to use use unstructured queries that can be parsed into a vector used as a search query. This type of retrieval has become more relevant as the previous boolean retrieval approaches provide unordered results instead of a ranked list of relevance. Other approaches have extended the boolean retrieval model by understanding what terms are closer together using a proximity operation which ensures that terms that are close in the query are close in the document (e.g. searching for &lt;code&gt;machine learning&lt;/code&gt; together)&lt;/p&gt;
&lt;p&gt;Even though ranked retrieval models can satisfy a user's information need by allowing them to prioritize and generalize the search result, boolean retrieval models are still used to this day as they provide fast and transparent results for ad hoc searches.  &lt;/p&gt;</description><category>boolean retrieval</category><category>information retrieval</category><category>inverse index</category><category>textbook</category><guid>posts/notes-on-information-retrieval-1/</guid><pubDate>Tue, 14 Mar 2023 23:20:00 GMT</pubDate></item></channel></rss>